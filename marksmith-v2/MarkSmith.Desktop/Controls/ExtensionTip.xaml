<UserControl
    x:Class="MarkSmith.Controls.ExtensionTip"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="RootControl">

    <!-- One-time tip card for the browser Connector. Styled as a plain theme card (same chrome as
         the pipeline cards) with the accent reserved for the glyph + CTA — no off-theme gradients,
         and text uses the theme's default type scale instead of hardcoded sizes. -->
    <Border CornerRadius="10" Margin="0,0,0,16" Padding="14,12"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
        <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="12">
            <FontIcon Glyph="&#xE774;" FontSize="18" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                      VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                <TextBlock Text="Work in your AI chat as normal" FontWeight="SemiBold" />
                <TextBlock Text="Install the MarkSmith Connector browser extension to send ChatGPT, Gemini and Claude replies straight here — then export a polished PDF whenever you're ready."
                           TextWrapping="Wrap" Opacity="0.75" />
            </StackPanel>

            <Button Grid.Column="2" Content="Get the extension" Click="OnGetExtensionClick"
                    Style="{ThemeResource AccentButtonStyle}" VerticalAlignment="Center" />

            <Button Grid.Column="3" Background="Transparent" BorderThickness="0" Click="OnCloseClick"
                    ToolTipService.ToolTip="Dismiss this tip"
                    VerticalAlignment="Center" Padding="4">
                <FontIcon Glyph="&#xE711;" FontSize="12" Opacity="0.8" />
            </Button>
        </Grid>
    </Border>
</UserControl>
