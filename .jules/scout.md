## 2024-05-24 - [Ollama Local Models path]
**Learning:** [The Ollama Local Models path is `~/Documents/Ollama` instead of `~/Documents/Ollama/Outputs`.]
**Action:** [Update the Ollama Local Models preset to use `Path.Combine(documents, "Ollama")` instead of `Path.Combine(documents, "Ollama", "Outputs")`.]
