## 2024-05-18 - Airgap Setup
**Learning:** Understanding the core web assets initialization process and where external dependencies might leak.
**Action:** The codebase correctly maps `marksmith.assets` and serves scripts from it (`WebAssets.cs`). I've verified `MarkdownHtmlService.cs` correctly loads them from the local virtual host (`Services.WebAssets.Mermaid`, `Services.WebAssets.KatexCss`, etc.). I will continue to double-check no external CDs are hardcoded and then run `dotnet build`.
**Learning:** `MarkSmith.Desktop` targets Windows which causes a build error on Linux via `.NET 10.0` inside this execution environment (Ubuntu-like OS in the sandbox). It requires `EnableWindowsTargeting` property to be set to true for successful build check.
**Action:** Since we're just checking the codebase, the error is an OS mismatch. The other projects (`Core`, `Tests`, `Cli`, `Wasm`) built successfully.
**Learning:** `MermaidHarvestService.cs` correctly uses `Services.WebAssets.Mermaid`. `MarkdownHtmlService.cs` uses local assets via `WebAssets`. Same for `MermaidDiagramStudioControl.xaml.cs`. Wasm app uses standard local blazor/bootstrap css/js. I don't see any external CDN calls!
**Action:** Wait, there's `mhchem.min.js` which is loaded as `<script defer src="{Services.WebAssets.Base}/mhchem.min.js"></script>`. And `<script defer src="{{Services.WebAssets.KatexJs}}"></script>`. Let's check `WebAssets.cs` again to ensure it doesn't have remote resources. And let's check `HtmlRenderTests.cs` if it tests rendering with a remote asset.
**Learning:** Verified the entire codebase using regex patterns `https://`, `http://`, `src=`, and `cdn`. There are absolutely zero external CDN fetches for web assets in `WebAssets.cs` or anywhere else involved in preview rendering. Everything uses `marksmith.assets` which is a local virtual host loading from bundled assets. The only external calls are for telemetry/license checking (`https://api.lemonsqueezy.com/v1/licenses/activate`) and plugin fetching, which is outside the preview rendering scope.
**Action:** The project is already airgapped correctly. All scripts use `marksmith.assets` internally. I'll just write down my findings and create the PR.
**Learning:** `dotnet test` fails some tests due to a missing `libSkiaSharp` binary in the Linux testing environment. SkiaSharp needs native assets for the running OS, which aren't present or compatible here. This is an environment issue, not a regression caused by a comment modification.
**Action:** The test failures are unrelated to the single comment added in `WebAssets.cs`. Proceed to the next step.
