## 2025-07-25 - Initial Profile
**Learning:** It seems there are no external remote fetches inside the `MdToPdf.Core/Services/MarkdownHtmlService.cs` or `MdToPdf.Core/Services/WebAssets.cs`. The only remote URLs found in `MarkdownHtmlService.cs` were `https://github.com...` and `http://www.w3.org/2000/svg`. However, I need to check the web assets themselves to make sure that they aren't fetching external remote scripts.
**Action:** Inspect the actual .js and .css files in `MdToPdf/Assets/web`.
**Learning:** Checked both `MdToPdf.Core/Services/MarkdownHtmlService.cs` and `marksmith-v2/MdToPdf.Core/Services/MarkdownHtmlService.cs`, and neither contains any CDN links like jsdelivr. All script and style tags appear to use `Services.WebAssets.*` references properly. Checked the web assets minified code and no `cdn.jsdelivr.net` is present, although some links in the minified code are standard comments or string literals pointing to Github repos.
**Action:** Verify `MdToPdf.Core/Services/WebAssets.cs` is not calling external CDN links.
**Learning:** Verified the `MdToPdf/Assets/web/katex.min.css` and it correctly uses relative URLs for its fonts (`url(fonts/...)`) without fetching from an external CDN. Also checked `MdToPdf/Assets/web` JS files. No cdn.jsdelivr.net or other external CDNs exist in the built assets, they just have comments holding original URLs for attribution. No telemetry scripts seem present. WebAssets properly injects script tags with `Services.WebAssets.Base` to serve from local host mapping or loopback.
**Action:** Add notes about the audit of the assets.
**Learning:** Checked `marketing/mock_gui.html`. As the directory name `marketing` implies, this is a web-based UI mockup likely used for landing pages, screenshots, or design iterations. It pulls in `marked.min.js` and `katex.min.js` via jsdelivr. Since this file is not bundled into the native application (`MdToPdf.Core` or `MdToPdf`), it does not violate the core airgap requirement for the application itself. But just to be perfectly aligned with Airgap's mission, I should perhaps replace them, or note that they are outside the scope of the app.
Airgapped marketing/mock_gui.html.
## 2025-07-25 - Verify Offline Preview Rendering
**Learning:** `dotnet build MdToPdf/MdToPdf.csproj` failed with `error NETSDK1100: To build a project targeting Windows on this operating system, set the EnableWindowsTargeting property to true.`. This is because the environment is Linux but the project targets `net8.0-windows10.0.19041.0`. We can rely on our static analysis which showed that all assets point to `https://marksmith.assets` mapped virtual host or local HTTP listener via `Services.WebAssets.Base` without any external HTTP/HTTPS calls.
**Action:** Proceed to prepare PR.
**Learning:** Confirmed `MdToPdf.Core/Services/MarkdownHtmlService.cs` and `marksmith-v2/MdToPdf.Core/Services/MarkdownHtmlService.cs` are almost identical (some layout tweaks, `marksmithWaitForExportReady` in v2). None of them have JS CDN calls or tracking scripts.
**Learning:** I ran `dotnet test marksmith-v2/tests/MdToPdf.Core.Tests` and got failures related to image decoding (libSkiaSharp missing on Linux test host), Mermaid stress tests, licensing mock failures, etc. None of these were caused by my changes which only updated marketing HTML. Pre-existing test failures are acceptable as long as my changes did not introduce new ones. My change is purely in `marketing/mock_gui.html` and adds offline assets.
**Learning:** Wrote and ran a headless Playwright script to verify `marketing/mock_gui.html` renders with offline mode active. The snapshot captured the page successfully, which proves it no longer relies on cdn.jsdelivr.net.
